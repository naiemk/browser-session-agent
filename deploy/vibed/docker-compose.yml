# Local stand-in for vibed-infra ui + api + gateway.
# Images are built by .github/workflows/docker.yml.
# Do not add a Chrome / Playwright service on the VPS — use compose.node.yml on the desktop.
include:
  - path: ../docker/compose.vps.yml
